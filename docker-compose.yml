version: '3.8'

services:
  esp32-builder:
    build: .
    volumes:
      - .:/workspace
      - ./build-output:/output
    environment:
      - GITHUB_REPOSITORY=yourusername/esp32-auto-ota
    working_dir: /workspace